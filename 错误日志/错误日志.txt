关于滚动条出现条件的判断，因为jq动画会出现错误
解决方法:
1，取消show（）的动画。。
2，将判断函数作为show的回调函数，完美解决
3，执行顺序引发的问题，在目标还没有完全展开的时候执行计算 造成的错误。

添加拖拽之后默认事件被禁用的问题

优化：删除页面的元素不再重新渲染页面，只修改并上传数组，只删除页面当前元素，减少了一次列表全部刷新
优化：增加页面的元素不再重新渲染页面，只修改并上传数组，只删除页面当前元素，再减少了一次列表全部刷新
因为不在刷新列表绑定监听事件。所以新增的页面按钮无法使用，解决方法：将监听事件从render页面刷新函数中封装后，重复调用。
删除文章之后因为页面元素的改变，所以无法打开首个文章的详情页，将监听事件函数在delete按钮事件后调用解决
